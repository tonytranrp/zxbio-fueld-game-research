# Procedural Hand Texture Overrides

Optional PNG overrides for the custom procedural robot-hand engine live here.

Preset files can reference texture paths for slots such as `shell`, `accent`, `joint`, `jointEdge`, `target`, and `palmPanel`. Missing files fall back to generated procedural textures.
