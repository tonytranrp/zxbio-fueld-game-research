# Research Verification Matrix

This matrix maps the current repository evidence to the remaining roadmap gaps in `research/pipeline_builder_cpp_research_plan.md`. It is release-facing evidence control, not an implementation plan by itself.

## Scope and source anchors

Source-of-truth research anchors with line ranges from `research/pipeline_builder_cpp_research_plan.md`:

- Lines 989-997, Phase 5 branch/join requirements: type-level branch node, predicate requirements, branch output validation, join validation, sequential branch execution, and branch diagnostics.
- Lines 998-1005, Phase 6 optional backend requirements: thread-pool executor plus oneTBB, Taskflow, stdexec prototypes, and a backend feature matrix.
- Lines 1006-1013, Phase 7 packaging/release requirements: install targets, package config files, examples, docs, CI compiler matrix, and v0.1.0 publication.
- Lines 1324-1336, acceptance criteria: sequential valid chains, compile-time invalid edge diagnostics, expected-like failure propagation, adapters, throwing-function runtime errors, GCC/Clang public-header coverage, compile-time benchmark records, and one success plus one failure example.
- Lines 1490-1501 and 1518, compile-time optimization notes: compile-time baselines/regression tracking and release-quality benchmark gates remain explicit goals.


## Worker 2 verification checkpoint — 2026-06-09

Fresh local verifier evidence on the integrated worker-1/3/4 team head:

- PASS — `cmake --preset clang-dev-ninja` configured the Clang developer preset.
- PASS — `cmake --build --preset clang-dev-ninja` completed the full build graph; this is the C++ typecheck gate for this repository.
- PASS — `ctest --preset clang-dev-ninja --output-on-failure` passed `250/250` tests with `0` failures, including adapter, runtime/result/diagnostics, branch/fan-in/thread-pool, compile-pass, compile-fail, CLI, package-smoke, and benchmark-smoke labels.
- PASS — `cmake --preset warnings-as-errors-ninja && cmake --build --preset warnings-as-errors-ninja` completed the full build graph under `PB_WARNINGS_AS_ERRORS=ON`; this is the practical lint/warning-clean gate for the C++ surfaces.

Scope boundary: this checkpoint is verification/spec evidence only. It does not promote cross-compiler validation, package-release artifacts, module compatibility beyond prior evidence, working external backends, async/sender coroutine backends, frozen external descriptor/export schema, or release publication. No production headers were edited for this checkpoint.

## Gap-to-evidence matrix

| Research area | Current implemented evidence | Current test/evidence surface | Release status | Next required slice |
| --- | --- | --- | --- | --- |
| Linear MVP core | Typed linear `pb::from<T>::then<S>::to<U>` DSL, stage traits, describe/metadata helpers, finalized-pipeline `stage_count` / `empty` constants, validation diagnostics, and public aliases. | Compile-pass/compile-fail suites, public-header smoke tests, examples, and `docs/roadmap-gap-map.md`. | Shipped MVP support. | Keep diagnostics wording hardened and refresh compile-fail evidence on each release candidate. |
| User adapters | Free-function, function-object, functor/member adapter surfaces. **Newer adapter surfaces:** `pb::runtime_callable<In,Out>` type-erased runtime-bound stateful callable, `pb::bind_callable` factory, `pb::c_function_stage<In,Out,Fn>` C-style function-pointer adapter (non-type template param), synchronous coroutine adapter (`pb::coro::sync_task<T>` + `pb::coroutine_stage<Stage>` / `pb::adapt_coroutine`), and a dependency-free synchronous sender/receiver scaffold (`pb::sync_just`, `pb::sync_sender_stage`) for one-value senders. | Adapter compile-pass/compile-fail tests; `tests/runtime/runtime_callable_adapter_smoke.cpp`; `tests/compile_pass/public_headers/adapt_runtime_callable.cpp`; `tests/runtime/coroutine_adapter_smoke.cpp`; `tests/compile_pass/public_headers/adapt_coroutine.cpp`; `tests/runtime/sender_receiver_adapter_smoke.cpp`; `tests/compile_pass/public_headers/adapt_sender_receiver.cpp`. | Shipped MVP support for all the above adapter shapes and the synchronous sender/receiver scaffold; full async/sender coroutine backends and working C++26 reflection adapter remain future work. | Keep adapter docs limited to supported callable shapes; add examples only when they match tested behavior. |
| Runtime result/error model | Sequential runtime, `run()` / `try_run()`, batch `try_run_each(...)` / `try_run_range(...)`, expected-like/result normalization, custom error conversion, observer callbacks, exception capture boundaries, a narrow sequential stage-storage policy namespace, **the parity-hardened five-wrapper engine-side error-policy DSL**, and **runtime-enforced `::with<pb::policy::errors::...>` DSL**: `pipeline` gained a 4th `Policies` type-list param; `pb::compile<P>(sequential{})` inspects it and wraps the engine in the matching error policy; `pb::has_error_policy_v<P>` is queryable. **Diagnostics axis:** `::with<diagnostics::verbose>` wraps the engine in `pb::with_verbose_diagnostics`; `pb::has_diagnostics_policy_v<P>`. **Copying axis:** `::with<copying::value/move_only/shared/clone>` carried + queryable; `pb::has_copying_policy_v<P>`. | All previous runtime smoke tests, parity matrix, composability, and std::expected lanes plus `tests/runtime/with_policy_dsl_runtime_smoke.cpp`, `tests/compile_pass/with_policy_dsl_compile_pass.cpp`, `tests/runtime/policy_axes_runtime_smoke.cpp`, `tests/compile_pass/policy_axes_compile_pass.cpp`, `include/pb/core/policy.hpp`, `include/pb/runtime/sequential.hpp` (4th Policies param). | Sequential MVP + wrapper-level error-policy DSL shipped with cross-wrapper `run()` / `try_run()` parity proven over linear, selected-output branch, and explicit fan-in topologies, plus runtime-enforced `::with<>` DSL for errors and diagnostics axes. Copying axis is carried + queryable; runtime enforcement is forward-looking. | Runtime enforcement of the copying axis, unified policy-surface narrative docs, and stable observer ABI/event schema remain roadmap-only. |
| Branch/join topology | Public `::branch<CaseA, CaseB>::to<Output>`, selected-output `::join<JoinStage>`, and fan-in `::fan_in<JoinStage>` / `::join_all<JoinStage>` DSLs with compile-time validation, runtime sequential and thread-pool fan-in execution, observer events, stateful storage, heterogeneous outputs, clone/projection policies. **New (wave-1/2):** `pb::unique_clone<T, Clone>` owned per-case deep-copy policy; fan-in observer lifecycle events (`on_fan_in_started` / `on_fan_in_case_scheduled` / `on_fan_in_case_completed` / `on_fan_in_completed`) — additive v1-ABI-safe no-op virtuals in `include/pb/runtime/observer.hpp`, emitted on sequential + thread-pool fan-in paths; `pb::fan_in_error_envelope` + `pb::collect_fan_in_errors` structured multi-error aggregation with stable `"pb.fan_in.errors.v1"` text schema; cooperative cancellation via `pb::cancellation_source` / `pb::cancellation_token` (schema `"pb.cancel.v1"`) — thread-pool fan-in checks the token before enqueueing each case and reports skipped cases via `on_case_skipped`; thread-pool fan-in example + stress test + runtime benchmark. | All previous branch/fan-in tests plus `tests/runtime/fan_in_observer_events_smoke.cpp`, `tests/runtime/fan_in_unique_clone_smoke.cpp`, `tests/compile_pass/public_headers/runtime_clone_unique.cpp`, `tests/runtime/fan_in_error_envelope_smoke.cpp`, `tests/compile_pass/public_headers/runtime_fan_in_error.cpp`, `tests/runtime/thread_pool_cancellation_smoke.cpp`, `tests/compile_pass/public_headers/runtime_cancellation.cpp`, `tests/runtime/thread_pool_fan_in_stress_smoke.cpp`, `examples/thread_pool_fan_in_demo.cpp`, `bench/runtime/thread_pool_fan_in.cpp`. | Supported for homogeneous outputs, variant-based heterogeneous outputs including duplicate output alternatives by index, type-list selected-output joins, move-only selected-branch input consumption, sequential and thread-pool fan-in aggregates with skipped/completed/failed states, diagnostics, void-output slots, borrowed move-only inputs, cooperative cancellation, fan-in lifecycle observer events, multi-error envelope, and `pb::shared_view` / `pb::projected` / `pb::unique_clone` clone-projection policies. | Keep preemptive (non-cooperative) cancellation, dependency backend fan-in, and broad backend branch execution as separate design/implementation phases. |
| Graph export | DOT/JSON/text helper evidence covers linear, selected-output branch, and explicit fan-in pipelines, including the `pb.core.graph.v1` typed schema-version contract, deterministic branch case identity fields, DOT label escaping, JSON string escaping, helper-output golden regressions, byte-equal extended goldens, `pb::export_text` compact text helper, and a substantially expanded `pb_cli` introspection CLI. | `pb_export_dot_compile_pass`, `pb_export_dot_branch_compile_pass`, `pb_export_json_compile_pass`, `pb_export_json_branch_compile_pass`, `pb_export_golden_compile_pass`, `tests/compile_pass/schema_v1_contract.cpp`, `tests/compile_pass/schema_v1_extended_golden.cpp`, `tests/compile_pass/export_text.cpp`, `tests/compile_pass/export_text_golden.cpp`, `tests/compile_pass/public_headers/export_text.cpp`, `tests/run_pb_cli_describe.cmake`, `tests/run_pb_cli_list.cmake`, `include/pb/core/export_text.hpp`, `tools/pb_cli.cpp`, `docs/export-schema-v1.md`, `docs/export-helper-schema.md`, `docs/graph-export-roadmap.md`. | Production-grade helper surface for supported shapes; the `pb.core.graph.v1` typed contract pins schema-version string and field layout via byte-equal goldens, but a frozen external interchange schema with stability promise and migration policy remains roadmap-only. | Promote helper-output golden regressions into release-grade compatibility fixtures with a v1 stability promise (no field removal without major bump) and a documented v1 → v2 migration path; expand the `pb_cli` surface to accept user-defined pipelines once that migration policy is decided. |
| Stable runtime descriptor | Current describe/metadata helpers support linear, selected-output branch, and explicit fan-in introspection, descriptor alias symmetry coverage, runtime `error_record` / `to_record(...)` diagnostic projection, `pb.diagnostics.v1` record/collector contract, indexed I/O aliases (`pipeline_stage_input_t<P, N>` / `pipeline_stage_output_t<P, N>`) with named out-of-range diagnostics, and tests that align descriptor stage identity with observer failure/exception callbacks. The `pb.core.graph.v1` typed schema-version contract anchors the descriptor/export surface to a versioned identifier, but no stable runtime descriptor/export *external* contract is promised. | `include/pb/core/describe.hpp`, `include/pb/core/pipeline.hpp`, `include/pb/runtime/error.hpp`, descriptor compile-pass tests, `tests/compile_pass/pipeline_stage_io_aliases.cpp`, `tests/compile_fail/pipeline_stage_input_index_oob.cpp`, `tests/compile_fail/pipeline_stage_output_index_oob.cpp`, `tests/compile_pass/schema_v1_contract.cpp`, runtime diagnostic smoke tests, `tests/compile_pass/diagnostics_contract.cpp`, `include/pb/core/diagnostics.hpp`, `docs/runtime-descriptor-roadmap.md`, `docs/export-schema-v1.md`. | Partial MVP introspection plus typed `pb.core.graph.v1` helper schema and narrow `pb.diagnostics.v1` diagnostic-record contracts; a frozen external descriptor/diagnostic artifact remains roadmap. | Define ownership, value schema, versioning policy, schema-migration rules (v1 → v2), diagnostic artifact export, and export compatibility before presenting descriptor output as a stable public interchange contract. |
| Optional backends | `thread_pool_backend` is supported for the current standard-library fan-in backend slice with cooperative cancellation. **New (wave-3):** compile-guarded optional backend integration seams — `include/pb/backends/{tbb,taskflow,stdexec}.hpp` — dormant on the default build; each exposes an ungated `*_backend_available_v` (false by default); `PB_ENABLE_{TBB,TASKFLOW,STDEXEC}` CMake options wired (default OFF). Working oneTBB, Taskflow, stdexec backends are NOT implemented. | `include/pb/runtime/thread_pool_backend.hpp`, `include/pb/backends/tbb.hpp`, `include/pb/backends/taskflow.hpp`, `include/pb/backends/stdexec.hpp`, `tests/compile_pass/backend_scaffolds_smoke.cpp` (static-asserts all `*_backend_available_v == false`), `pb_runtime_thread_pool_smoke`, `pb_runtime_thread_pool_fan_in_smoke`, `pb_runtime_backend_feature_matrix_smoke`, fail-fast configure checks for `PB_BACKEND_TASKFLOW`, `PB_BACKEND_TBB`, and `PB_BACKEND_STDEXEC`, `docs/optional-backends-roadmap.md`. | Supported for the fan-in thread-pool slice (with cooperative cancellation). Dormant compile-guarded seams exist for TBB/Taskflow/stdexec — working external backends are NOT shipped. | Activate a working backend behind `PB_ENABLE_TBB` only after `find_package(TBB)` integration, isolation tests, and matrix evidence exist. |
| C++26 feature gates and reflection adapter | Typed `pb::features::*` `inline constexpr bool` gate constants ship for `has_cpp26`, `has_reflection`, `has_contracts`, `has_pack_indexing`, `has_std_expected`, and `has_deducing_this`, alongside their `PB_HAS_*` macro counterparts with user-override support and implication invariants. `pb::reflect_stage<T>` ships as a gated forward-compat scaffold in `include/pb/adapt/reflect.hpp`. | `include/pb/core/cpp26_features.hpp`, `include/pb/adapt/reflect.hpp`, `tests/compile_pass/cpp26_features_fallback.cpp`, `tests/compile_pass/reflect_stage_gate.cpp`, `docs/cpp26-feature-gates.md`. | Typed gate constants + fallback-tested forward-compat seam shipped; C++26 reflection is **not** supported on current toolchains. | Add a working reflection-driven adapter once a C++26 reflection toolchain baseline is established, plus broader compiler/library detection tests and release-grade C++26 feature-gate validation in CI. |
| C++20 named module | `PB_BUILD_MODULE` CMake option; `include/pb/pipeline.mpp` built via `FILE_SET CXX_MODULES`; `modules-ninja` preset; `tests/module/use_module.cpp` does `import pb.pipeline;` and passes as `pb_use_module`. Verified on clang 22 (llvm-mingw), CMake 4.3, Ninja. Default header build unaffected. | `docs/modules.md`, `CMakePresets.json` (`modules-ninja` preset), `include/pb/pipeline.mpp`, `tests/module/use_module.cpp`. | Shipped for the documented preset on clang 22. Default header-mode build unaffected. | Broader compiler compatibility evidence; install/export story for module consumers. |
| Benchmark thresholds and compile-time budgets | Compile-time/header smoke targets are available in test-enabled developer presets. **New (wave-1):** branch/fan-in compile-time benchmark target `bench/compile_time/branch_fan_in_10.cpp` and thread-pool fan-in runtime benchmark `bench/runtime/thread_pool_fan_in.cpp` added. Clang time-trace workflow exists, but no release threshold or regression budget is enforced. | `bench/compile_time/branch_fan_in_10.cpp`, `bench/runtime/thread_pool_fan_in.cpp`, `docs/benchmark-workflow.md`, `docs/benchmark-evidence-template.md`, `docs/benchmark-baselines.md`, CTest labels `benchmark`, `compile_time`, `header`, `bench`. | Reproducible smoke baseline scaffolding exists; measured budgets are not enforced. | Record release-candidate timing numbers and add CI/regression budget policy before treating performance as a release gate. |
| Release governance | CMake install/package config smoke scaffolding, release checklist, and cross-compiler validation workflow exist. Latest GitHub code-validation evidence passed for GCC C++20/C++23, Clang C++20/C++23, MSVC C++20, and clean Ubuntu package-release on SHA `87299c14c813753d170911239e251064cbbfee6f`; the same SHA also passed local Clang developer and package-release verification, plus normal CI Clang dev/package/benchmark lanes. Current local verifier checkpoint on the integrated team head: **250/250 ctest** on `clang-dev-ninja`; warnings-as-errors build passed. `modules-ninja` compatibility remains prior evidence, not refreshed in this checkpoint. | `docs/release-readiness-checklist.md`, `docs/release-evidence-template.md`, `docs/cross-compiler-validation.md`, `.github/workflows/cross-compiler-validation.yml`, package presets, `pb_package_config_smoke`. | Release scaffolding plus exact code-SHA local and GitHub matrix evidence are current for SHA `87299c14c813753d170911239e251064cbbfee6f`. Cross-compiler validation not yet rerun on the integrated team head. | Record compiler/preset/archive/run evidence in release notes, rerun cross-compiler workflow on the integrated team head, then tag. |

## Active priority backlog for release coordination

The docs lane tracks these priorities so release notes and PR text do not overclaim in-progress work from other workers:

| Priority | Research-plan anchor | Status to claim today | Required evidence before promotion |
| --- | --- | --- | --- |
| 1 | Cross-compiler validation rerun | Not yet run on the integrated team head (250/250 ctest local; warnings-as-errors build passed). | Rerun GitHub `Cross Compiler Validation` workflow on the integrated team head. |
| 2 | Phase 2 runtime result/error and expected-like behavior | Sequential MVP + parity-hardened five-wrapper error-policy DSL + runtime-enforced `::with<pb::policy::errors::...>` DSL (errors + diagnostics axes), cooperative cancellation (`pb.cancel.v1`), fan-in multi-error envelope (`pb.fan_in.errors.v1`). | Runtime enforcement of the copying axis, full policy-surface narrative docs, and stable observer ABI/event schema. |
| 3 | Compile-time optimization notes, lines 1490-1501 and 1518 | Benchmark smoke scaffolding + branch/fan-in compile-time benchmark + thread-pool fan-in runtime benchmark. | Baseline records for 5-stage, 50-stage, and branch/fan-in chains plus context. |
| 4 | Runtime stage ownership/lifetime assumptions | Stateful storage DSL (`pb.state.v1`) including thread-local storage policy (`pb::with_thread_local_state<State>`). All four wrapper factories (`pb::with_state`, `pb::with_borrowed_state`, `pb::with_shared_state`, `pb::with_reset_per_run_state`) ship. | External-resource cleanup policy; public lifetime diagnostics for misuse beyond `current_state<T>()` outside-scope; descriptor-record entry for stateful wrappers. |
| 5-6 | Phase 5 branch output and join validation | Accepted validation evidence exists; keep it scoped to validation, not execution. | Final candidate branch includes the validation commits plus branch-output and join-validation compile-pass/compile-fail logs. |
| 7 | Phase 5 sequential and thread-pool branch/fan-in execution | Supported for the full wave-1/2/3 fan-in surface: unique_clone, multi-error envelope, lifecycle events, cooperative cancellation, thread-pool fan-in example + stress + bench. | Additional backend branch execution, preemptive cancellation policy, and broader ownership/lifetime policies need design, tests, and docs before promotion. |
| 8-9 | Descriptor/export and graph export themes | Compile-time metadata + DOT/JSON/text helpers + `pb::export_text` + `pb_cli` with five built-in pipelines + `pb::tooling::pipeline_registry`; typed `pb.core.graph.v1` contract. Frozen external schema, CLI/file export for arbitrary user pipelines remain roadmap-only. | Stability promise, schema-migration policy, broader CLI surface for user pipelines, descriptor-backed compatibility fixtures. |
| 10 | Phase 6 optional backends | Sequential + thread-pool fan-in supported + cooperative cancellation. Dormant compile-guarded seams for TBB/Taskflow/stdexec exist but no working backends. | Working backend activation with `find_package`, isolation tests, and matrix evidence before broader backend claims. |
| 11 | Adapters — new surfaces | Runtime-bound callable (`pb::runtime_callable`), C-style function-pointer (`pb::c_function_stage`), synchronous coroutine adapter (`pb::coro::sync_task<T>` / `pb::coroutine_stage`), and the dependency-free synchronous sender/receiver scaffold (`pb::sync_just`, `pb::sync_sender_stage`) are shipped and tested. | Full async/sender coroutine backend; working C++26 reflection adapter; full overloaded/ref-qualified member handling. |
| 12 | C++20 named module | `PB_BUILD_MODULE` + `modules-ninja` preset + `import pb.pipeline;` verified on clang 22. | Broader compiler compatibility; install/export story for module consumers. |
| 13-14 | Phase 7 compiler/release requirements | GitHub matrix evidence exists for SHA `87299c14c813753d170911239e251064cbbfee6f`: GCC C++20/C++23, Clang C++20/C++23, MSVC C++20, and clean Ubuntu package-release all passed 163/163. | Rerun on the integrated team head with exact-SHA compiler ID/version plus configure/build/test/package logs before tagging. |

## Runtime lane gap/evidence refresh

| Area | Moved from | Current status | Evidence | Still missing |
| --- | --- | --- | --- | --- |
| Optional backends | Missing / placeholder CMake flags only | Partial support: feature matrix, fail-fast reserved external backend flags, standalone `thread_pool`, and supported `thread_pool_backend` fan-in scheduling with deterministic ordered aggregation + cooperative cancellation. **New:** dormant compile-guarded seams for TBB (`include/pb/backends/tbb.hpp`), Taskflow (`include/pb/backends/taskflow.hpp`), stdexec (`include/pb/backends/stdexec.hpp`); `*_backend_available_v == false` on default build; `PB_ENABLE_{TBB,TASKFLOW,STDEXEC}` CMake options wired. | `include/pb/runtime/thread_pool_backend.hpp`, `include/pb/backends/{tbb,taskflow,stdexec}.hpp`, `tests/compile_pass/backend_scaffolds_smoke.cpp`, `pb_runtime_thread_pool_smoke`, `pb_runtime_thread_pool_fan_in_smoke`, `pb_runtime_backend_feature_matrix_smoke`, fail-fast configure checks | Working oneTBB, Taskflow, stdexec backends; backend examples; broader cancellation/error policy; dependency isolation target tests; backend benchmarks |
| Policy DSL | Missing full DSL | **Three axes shipped:** errors axis (five runtime-enforced markers, `compile<>` selects wrapper; `pb::has_error_policy_v<P>`), diagnostics axis (`verbose` selects `with_verbose_diagnostics` wrapper; `pb::has_diagnostics_policy_v<P>`), copying axis (four markers, carried + queryable; `pb::has_copying_policy_v<P>`; runtime enforcement is forward-looking). `pipeline` 4th `Policies` type-list param carries all three axes. | `include/pb/core/policy.hpp`, `include/pb/runtime/sequential.hpp`, `tests/runtime/with_policy_dsl_runtime_smoke.cpp`, `tests/compile_pass/with_policy_dsl_compile_pass.cpp`, `tests/runtime/policy_axes_runtime_smoke.cpp`, `tests/compile_pass/policy_axes_compile_pass.cpp`, all parity-matrix / composability / std::expected lanes | Runtime enforcement of the copying axis; executor capability policy; assertion/contract policy; policy conflict diagnostics |
| Fan-in lifecycle | Missing | **Shipped (wave-1).** Four additive v1-ABI-safe no-op virtual defaults in `include/pb/runtime/observer.hpp`: `on_fan_in_started`, `on_fan_in_case_scheduled`, `on_fan_in_case_completed`, `on_fan_in_completed`. Emitted on sequential and thread-pool fan-in paths. | `include/pb/runtime/observer.hpp`, `tests/runtime/fan_in_observer_events_smoke.cpp`, `tests/runtime/thread_pool_fan_in_stress_smoke.cpp` | Stable observer ABI/event schema promise; observer overhead benchmarks |
| Fan-in multi-error envelope | Missing | **Shipped (wave-2).** `pb::fan_in_error_envelope` + `pb::collect_fan_in_errors`, stable schema `"pb.fan_in.errors.v1"`. | `include/pb/runtime/fan_in_error.hpp`, `tests/runtime/fan_in_error_envelope_smoke.cpp`, `tests/compile_pass/public_headers/runtime_fan_in_error.cpp` | Broader error-envelope policy knobs; backend scheduling/trace export |
| Cooperative cancellation | Missing | **Shipped (wave-2).** `pb::cancellation_source` / `pb::cancellation_token` over `std::atomic<bool>`, schema `"pb.cancel.v1"`. Thread-pool fan-in checks token before enqueueing each case. | `include/pb/runtime/cancellation.hpp`, `tests/runtime/thread_pool_cancellation_smoke.cpp`, `tests/compile_pass/public_headers/runtime_cancellation.cpp` | Preemptive (non-cooperative) cancellation is documented as out of scope |
| `pb::unique_clone<T, Clone>` | Missing ("owned per-case unique-clone beyond shared-view") | **Shipped (wave-1).** Owned per-case deep-copy fan-in policy. | `include/pb/runtime/clone.hpp`, `tests/runtime/fan_in_unique_clone_smoke.cpp`, `tests/compile_pass/public_headers/runtime_clone_unique.cpp` | Runtime enforcement of the copying policy axis (forward-looking) |
| Runtime-bound callable adapters | Missing | **Shipped (wave-3).** `pb::runtime_callable<In,Out>` (type-erased, owns callable), `pb::bind_callable` (factory), `pb::c_function_stage<In,Out,Fn>` (C-style function-pointer non-type template param). | `include/pb/adapt/runtime_callable.hpp`, `tests/runtime/runtime_callable_adapter_smoke.cpp`, `tests/compile_pass/public_headers/adapt_runtime_callable.cpp` | Full overloaded/ref-qualified member handling; reference-lifetime adapter |
| Synchronous coroutine adapter | Missing | **Shipped (wave-2).** `pb::coro::sync_task<T>` eager synchronous task + `pb::coroutine_stage<Stage>` / `pb::adapt_coroutine`. Async/sender backends remain future work. | `include/pb/adapt/coroutine.hpp`, `tests/runtime/coroutine_adapter_smoke.cpp`, `tests/compile_pass/public_headers/adapt_coroutine.cpp` | Suspending awaiter / async coroutine for sender-receiver backends |
| C++20 named module | Missing | **Shipped (wave-3).** `PB_BUILD_MODULE` + `modules-ninja` preset + `import pb.pipeline;` passing on clang 22. Default header build unaffected. | `docs/modules.md`, `include/pb/pipeline.mpp`, `CMakePresets.json`, `tests/module/use_module.cpp` | Broader compiler compatibility; install/export story |
| Optional backend seams (TBB/Taskflow/stdexec) | Missing / roadmap | **Dormant scaffold shipped (wave-3).** Compile-guarded headers; `*_backend_available_v == false`; `PB_ENABLE_*` CMake options wired. `tests/compile_pass/backend_scaffolds_smoke.cpp` verifies dormancy. | `include/pb/backends/{tbb,taskflow,stdexec}.hpp`, `tests/compile_pass/backend_scaffolds_smoke.cpp` | Working backends require `find_package` integration, isolation tests, and matrix evidence |
| `pb::tooling::pipeline_registry` | Missing | **Shipped (wave-2).** Name-keyed insertion-ordered registry; pb_cli refactored; 5 built-in pipelines. | `include/pb/tooling/cli_registry.hpp`, `tools/pb_cli.cpp`, `tests/run_pb_cli_list.cmake` | Stable CLI contract for arbitrary user-defined pipelines |
| Stateful stage storage | Partial / not a public guarantee | Done: all four wrapper factories + thread-local policy (`pb::with_thread_local_state<State>`) shipped. `pb.state.v1` schema-pinned. | `include/pb/runtime/state.hpp`, `tests/runtime/state_dsl_smoke.cpp`, `tests/runtime/state_thread_local_smoke.cpp`, `docs/state-dsl.md` | External-resource cleanup policy; descriptor-record entry for stateful wrappers |
| Standard-library runtime boundary | At risk from optional-backend placeholders | Done for this slice: runtime remains standard-library-only; external dependency backend flags fail fast; new dormant scaffold seams are fully `#if PB_HAS_*`-guarded. | Dependency grep, fail-fast configure checks, `include/pb/backends/*.hpp` compile-guard | Future dependency backend targets must keep external dependencies isolated from core/runtime public headers |

## Current release decision rule

A release note may claim the linear/sequential MVP, batch `try_run_each` / `try_run_range` helpers, finalized-pipeline `stage_count` / `empty` constants, current adapter shapes (including `pb::runtime_callable`, `pb::c_function_stage`, synchronous `pb::coroutine_stage`, and the dependency-free synchronous `pb::sync_sender_stage` / `pb::sync_just` sender/receiver scaffold), smoke benchmark scaffolding (including branch/fan-in compile-time and thread-pool fan-in runtime benchmarks), package-consumer scaffolding, supported sequential branch execution, variant-based heterogeneous branch outputs including duplicate alternatives by index, move-only selected-branch input consumption, explicit fan-in joins, thread-pool backend fan-in scheduling with cooperative cancellation (`pb.cancel.v1`), `pb::shared_view` / `pb::projected` / `pb::unique_clone` clone-projection policies, fan-in multi-error envelope (`pb.fan_in.errors.v1`), fan-in observer lifecycle events, the five-wrapper error-policy DSL (`throwing` / `terminating` / `ignoring` / `propagating` / `verbose` engines), runtime-enforced `::with<pb::policy::errors::...>` and `::with<pb::policy::diagnostics::verbose/quiet>` DSL axes (with `pb::has_error_policy_v<P>` and `pb::has_diagnostics_policy_v<P>`), `::with<pb::policy::copying::...>` axis (carried + queryable), the typed `pb.core.graph.v1` schema-version contract with byte-equal goldens, `pb::export_text` compact text helper, `pb_cli` introspection with `pb::tooling::pipeline_registry` and five built-in pipelines, indexed pipeline I/O aliases with named OOB diagnostics, the narrow `pb.diagnostics.v1` record/collector contract, typed `pb::features::*` C++26 feature-gate constants with regression-tested fallback, the gated `pb::reflect_stage<T>` reflection-adapter scaffold, stateful storage DSL (`pb.state.v1`) including thread-local storage policy, dormant optional backend integration seams (TBB/Taskflow/stdexec compile-guarded scaffolds), C++20 named module build (`modules-ninja` preset, `pb_use_module` passing on clang 22), and the GCC/Clang/MSVC/package validation matrix for exact code SHA `87299c14c813753d170911239e251064cbbfee6f`. Current local verifier checkpoint on the integrated team head: **250/250 ctest** on `clang-dev-ninja`; warnings-as-errors build passed. `modules-ninja` compatibility remains prior evidence, not refreshed in this checkpoint.

It must keep working oneTBB/Taskflow/stdexec backends, preemptive (non-cooperative) cancellation, runtime enforcement of the copying policy axis, full async/sender coroutine backends beyond the synchronous scaffold, frozen external descriptor/export schema, CLI/file export for arbitrary user pipelines, benchmark thresholds, stable observer ABI/event schema, exported diagnostic artifacts or frozen diagnostic wording/schema migrations, working C++26 reflection adapter on a real toolchain, CI-enforced performance budgets, and cross-compiler validation on the integrated team head marked as roadmap gaps. Cross-compiler validation has **not** yet been rerun on the integrated team head; rerun before tagging.

## Evidence refresh checklist

Before changing any row from roadmap/partial to shipped support, require all three:

1. Code/API evidence in the owned surface.
2. Targeted compile-pass, compile-fail, runtime, benchmark, or package tests proving the behavior.
3. Documentation/examples that state the boundary without promoting future roadmap behavior.
