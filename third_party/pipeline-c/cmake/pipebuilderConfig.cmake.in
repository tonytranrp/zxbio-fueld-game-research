@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/pipebuilderTargets.cmake")

if(NOT TARGET pb::pipeline)
  add_library(pb::pipeline INTERFACE IMPORTED)
  set_target_properties(pb::pipeline PROPERTIES
    INTERFACE_LINK_LIBRARIES pb::runtime
  )
endif()

check_required_components(pipebuilder)
