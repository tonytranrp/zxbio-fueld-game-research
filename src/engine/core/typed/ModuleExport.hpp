#pragma once

// Marker consumed by cmake/GenerateTypedRegistries.cmake. Module headers invoke
// this macro with category, registry alias, and module type.
#define BIOFUEL_TYPED_MODULE(CATEGORY, REGISTRY_ALIAS, MODULE_TYPE)
